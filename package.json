{
  "name": "orion-x",
  "private": true,
  "version": "0.0.0",
  "description": "Workflow orchestration foundation with event-oriented execution.",
  "scripts": {
    "lint": "sh -c \"echo lint placeholder\"",
    "test": "sh -c \"echo no tests yet\"",
    "build": "sh -c \"echo build placeholder\"",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.12"
  }
}
