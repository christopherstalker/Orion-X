# Screenshot Guidance

This repository does not store binary screenshots.

To capture screenshots for local demos or release notes:

1. Start the stack (`make up`, `make dev`).
2. Open the UI (`http://localhost:3000`).
3. Navigate to a representative workflow run screen.
4. Capture using your OS screenshot tool.
5. Attach screenshots to PR descriptions or external documentation.
